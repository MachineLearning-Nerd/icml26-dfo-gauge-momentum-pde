# Branch audit companion

The authoritative branch record is in
[BRANCH_AUDIT.md](BRANCH_AUDIT.md). The final state is deliberately
main-only: the remote default branch, local branch set, and reachable commit
identities are checked by <code>verify_final.py</code>.

